{
  "name": "tutorial-vault",
  "version": "0.2.0",
  "private": true,
  "description": "Gestor personal de tutoriales con Node y SQLite",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^2.0.0",
    "sqlite3": "^6.0.1"
  },
  "overrides": {
    "path-to-regexp": "0.1.13"
  }
}
